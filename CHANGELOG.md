# 更新日志 / Changelog

## 0.3.0（2026-08-21）

主要调整内容：配线图模块。

### 新增与调整

- 页面排版重新布局，界面美化。
- 预设基础元素新增控制点编辑：拖动四角控制点调整大小，Shift+拖动等比缩放，拖动圆角控制点调整圆角大小；边框粗细可调。
- 属性面板布局调整。
- 设置「默认」标签页新增更多默认参数：形状外观（默认填充 / 描边 / 描边粗细 / 圆角）、形状与全部对象不透明度、道岔默认长度与开口幅度。
- 双线道岔「开合角度（°）」参数调整为「开口大小（px）」。
- 元件库线路站点的物理站映射建议支持收起。

### 修复

- 修复点击配线图「筛选」弹出菜单导致空白页的问题。
- 调整部分撤销逻辑。
- 修复调整双线道岔开合角度后轨宽变化的问题。

## 0.2.1（2026-08-20）

### 修复

- 修复点击配线图「筛选」弹出菜单导致空白页的问题：PopoverMenu 曾在列表回调里调用 `useState`（section 折叠状态），弹层开合改变 React hook 数量触发 "Rendered more hooks" 崩溃；已把 section 抽成独立组件。

## 0.2.0（2026-08-20）

### 新增

- 配线图新增无限流画布与标准画布切换，加入画布大小调整功能。
- 新增设置按钮，并收纳部分设置项。
- 站台新增调整锚点：默认左侧端点，新增中间、右侧，以及上/中/下九宫格选择。
- 自定义站台长度可调范围加大。
- 吸附模式的吸附数值可调。
- 新增默认设置（可设置默认站台长度、线路间距等）。
- 框选多个元件时新增批量设置：按分类（区间与车站 / 道岔与连接 / 场段）统一设置共有模板参数。
- 新增元件复制功能（带参数）：Ctrl+C / Ctrl+V / Ctrl+D 或工具栏按钮；单元件复制不含连接，框选复制保留模块间连接。

### 修复

- 自动避让存在会反复推走斜向站名标签的问题，现**默认关闭**；需要时可使用工具栏「🔀 避让一次」手动触发。
- 修复站台堆叠时调整 z-index 只有站台生效、线路没有生效的问题。
- 修复侧式站台无法自定义线路间距的问题。
- 修复斜向（45°）站名标签在重新进入编辑器、或编辑元件（改参数 / 切模板）时会重置位置的问题。
